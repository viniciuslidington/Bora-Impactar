server {
    listen 80;
    server_name localhost;

    # Configuração para o frontend
    location / {
        proxy_pass http://frontend:3007;  # Nome do serviço no Docker Compose
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
